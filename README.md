## Tech stack
* **Core:** React, Vite
* **UI:** Material UI (MUI)
* **Data:** Axios
* **Auth:** Firebase Authentication (REST API)

## Project run
```
npm install
npm run dev
```

## Environment Setup (.env)
To enable authentication, create a `.env` file in the root directory and add the API key in pdf